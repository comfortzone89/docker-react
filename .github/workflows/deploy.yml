name: Deploy Frontend
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker build -t shakenbake89/docker-react -f Dockerfile.dev .
      - run: docker run -e CI=true shakenbake89/docker-react npm test
